export const LoadingDots = ({ size=20, display='inline' }) => {
  return <span style={{fontSize: size, display: display}} className='text-center typing-dots'><span>.</span><span>.</span><span>.</span></span>
}
